akismet.apiKey:
  links:
    - name: Encrypted String
      url: /docs/config/encrypted-string

akismet.enable:

akismet.fields.author:

akismet.fields.authorEmail:

akismet.fields.authorUrl:
  example: site

akismet.fields.content:
  example: comment

akismet.siteUrl:
  example: https://extrastatic.xyz

akismet.type:

allowedFields:
  example: '["author","email","comment","site"]'

branch:

commitMessage:
  example: "add new comment by {fields.author} <Stapsher>"
  links:
    - name: Placeholders
      url: /docs/config/placeholders

extension:
  example: yaml

filename:
  example: "{_date~unix}.{_id}"
  links:
    - name: Placeholders
      url: /docs/config/placeholders

format:
  example: yaml

generatedFields:
  example: "\ntimestamp:\n  type: date\n  options:\n    format: unix"
  links:
    - name: Generated Fields
      url: /docs/config/generated-fields

moderation:

path:
  example: "data/comments"
  links:
    - name: Placeholders
      url: /docs/config/placeholders

pullRequestBody:

recaptcha.enable:

recaptcha.secretKey:
  links:
    - name: Encrypted String
      url: /docs/config/encrypted-string

requiredFields:
  example: '["author","comment","email"]'

transforms:
  example: "\nemail: hash~md5"
